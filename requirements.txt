# Aldyro GEO Audit Terminal — proxy engine dependencies
# Compatible-release ranges keep the build reproducible while allowing patches.
# Run `pip freeze > requirements.lock.txt` after a green staging deploy and ship
# the lock file if you want fully deterministic builds.

fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
google-genai>=1.0,<2.0
pydantic>=2.9,<3.0
